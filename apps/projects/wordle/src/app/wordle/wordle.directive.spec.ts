import { WordleDirective } from './wordle.directive';

describe('WordleDirective', () => {
  it('should create an instance', () => {
    const directive = new WordleDirective();
    expect(directive).toBeTruthy();
  });
});
